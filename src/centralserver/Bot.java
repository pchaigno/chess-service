package centralserver;

/**
 * Represent a bot.
 */
public class Bot extends Resource {

	/**
	 * Constructor
	 * @param uri The URI.
	 * @param name The name.
	 */
	public Bot(String uri, String name) {
		super(uri, name);
	}
}